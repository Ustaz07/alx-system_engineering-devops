#!/usr/bin/env bash
# Install nginx on your web-01 & querying Nginx

sudo apt-get update
sudo apt install -y nginx
# sudo service nginx status
sudo ufw allow 'Nginx HTTP'

echo "Hello World!" | sudo tee /var/www/html/index.html

sudo nginx -t

sudo service nginx restart
